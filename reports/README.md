# Reports (UpToWes)

Diretriz: reports gerados a partir do acervo **podem conter trechos do conteúdo** (copyright/PI/IP).
Por padrão, os scripts escrevem outputs em `dataset/out/reports/` (ignored no Git).

Arquivo seed versionável:
- `reports/queries_pilot_seed.json` (apenas lista de queries)

Outputs gerados (não versionar):
- `dataset/out/reports/queries_pilot.md`
